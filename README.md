# Linq
LINQ is a lightweight Python LAN discovery tool for monitoring devices on a local network. It detects IP addresses, MAC addresses, device names and status through a simple terminal interface. Designed for network administration and home labs. Ethernet is recommended, as Wi-Fi support is currently experimental and may be unreliable.
